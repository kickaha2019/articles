The "HTML" declaration embeds the given text straight into the
article page without any additional processing.  The given text
is assumed to be valid HTML code.

Example:

~~~
HTML:
	<i>Hello World</i>
	
~~~
Which gives:

<i>Hello World</i>
